# slice

## slice底层结构

```go
type slice struct {
	array unsafe.Pointer // 元素指针
	len int // 长度
	cap int // 容量
}
```

## 扩容机制

原容量小于256的时候双倍扩容，大于256的时候是追加四分之一原大小再加192

## 什么时候会修改原切片

1.  截取切片后修改时，未发生扩容时的修改
2.  作为参数传递时，底层数组被修改，但也同样需要注意扩容时机，未触发扩容的修改才会导致原切片被修改

